# CMake snippet for fast laser heating validation test
# Add this to tests/integration/CMakeLists.txt or main CMakeLists.txt

# Fast laser heating validation test (runtime: ~30 seconds)
cuda_add_executable(test_laser_heating_fast
    tests/integration/test_laser_heating_fast.cu
    src/physics/thermal/thermal_lbm.cu
    src/physics/laser/laser_source.cu
    src/physics/materials/material_properties.cu
    src/physics/phase_change/phase_solver.cu
    src/lattice/lattice_d3q7.cu
)

target_link_libraries(test_laser_heating_fast
    ${CUDA_LIBRARIES}
    gtest
    gtest_main
    pthread
)

target_include_directories(test_laser_heating_fast PRIVATE
    ${CMAKE_SOURCE_DIR}/include
    ${CMAKE_SOURCE_DIR}/src
)

set_target_properties(test_laser_heating_fast PROPERTIES
    CUDA_SEPARABLE_COMPILATION ON
    CUDA_ARCHITECTURES "60;70;75;80"
)

# Add to CTest if desired
add_test(NAME LaserHeatingFastValidation COMMAND test_laser_heating_fast)
set_tests_properties(LaserHeatingFastValidation PROPERTIES
    TIMEOUT 60
    LABELS "fast;validation;laser;thermal"
)
